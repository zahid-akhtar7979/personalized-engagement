apiVersion: v1
kind: Service
metadata:
  name: engagement
  namespace: pep-sit
  labels:
    app: engagement
    environment: sit
spec:
  type: ClusterIP
  selector:
    app: engagement
  ports:
    - name: http
      port: 8083
      targetPort: http
      protocol: TCP
