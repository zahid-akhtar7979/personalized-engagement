apiVersion: v1
kind: Service
metadata:
  name: dataset-replay
  namespace: pep-sit
  labels:
    app: dataset-replay
    environment: sit
spec:
  type: ClusterIP
  selector:
    app: dataset-replay
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
