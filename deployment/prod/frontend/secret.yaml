# Template — frontend static assets have no runtime secrets.
# Use for optional basic-auth or TLS materials if terminating TLS in-pod.
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: pep-prod
  labels:
    app: frontend
    environment: prod
type: Opaque
stringData:
  PLACEHOLDER: "unused"
