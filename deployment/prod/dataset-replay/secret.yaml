# Template — dataset-replay has no sensitive env vars by default.
# Retained for consistency; delete PLACEHOLDER if unused.
apiVersion: v1
kind: Secret
metadata:
  name: dataset-replay-secret
  namespace: pep-prod
  labels:
    app: dataset-replay
    environment: prod
type: Opaque
stringData:
  PLACEHOLDER: "unused"
