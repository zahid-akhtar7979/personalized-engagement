apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dataset-replay-data
  namespace: pep-prod
  labels:
    app: dataset-replay
    environment: prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard  # Uncomment and set for your cluster
