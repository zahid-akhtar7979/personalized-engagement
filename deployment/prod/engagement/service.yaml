apiVersion: v1
kind: Service
metadata:
  name: engagement
  namespace: pep-prod
  labels:
    app: engagement
    environment: prod
spec:
  type: ClusterIP
  selector:
    app: engagement
  ports:
    - name: http
      port: 8083
      targetPort: http
      protocol: TCP
