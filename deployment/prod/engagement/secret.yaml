# Template — engagement has no sensitive env vars by default.
apiVersion: v1
kind: Secret
metadata:
  name: engagement-secret
  namespace: pep-prod
  labels:
    app: engagement
    environment: prod
type: Opaque
stringData:
  PLACEHOLDER: "unused"
